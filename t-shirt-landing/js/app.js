function printMessage(username, badgeCount, points){

var message = username + " has" + badgeCount + " total badge(s) and " + points + " point in Javascript";
  
  console.log(message);
}


//connect the API URL
//Read the data
//parase the data
//print the data 

printMessage("Andrew", 1000, 100000);